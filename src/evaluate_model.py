"""
Loads the model and processed data, computes predictions on the test set, calculates metrics like MSE and R^2, 
and can create simple plots saved into reports/figures/ (e.g., predicted vs actual returns)
"""